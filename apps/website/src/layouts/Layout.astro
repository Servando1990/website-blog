---
import '../styles/global.css';

export interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogUrl?: string;
  ogType?: string;
  ogSiteName?: string;
}

const {
  title,
  description = 'Founder-led software partner for private capital teams that need clearer workflows, better internal systems, and delivery that holds up in practice.',
  ogImage = '/api/og/home.png',
  ogUrl = Astro.url.href,
  ogType = 'website',
  ogSiteName = 'controlthrive',
} = Astro.props;

const baseUrl = (Astro.site || Astro.url.origin).toString();
const ogVersion =
  process.env.VERCEL_GIT_COMMIT_SHA ||
  process.env.VERCEL_DEPLOYMENT_ID ||
  process.env.OG_IMAGE_VERSION ||
  'local';
const resolvedOgImageUrl = new URL(ogImage, baseUrl);
resolvedOgImageUrl.searchParams.set('v', ogVersion);
const resolvedOgImage = resolvedOgImageUrl.href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=controlthrive-logo" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={ogUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={resolvedOgImage} />
    <meta property="og:site_name" content={ogSiteName} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={ogUrl} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={resolvedOgImage} />

    <meta name="theme-color" content="#FFFFFF" />

    <script async is:inline src="https://www.googletagmanager.com/gtag/js?id=G-LLMXBYB3P8"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());
      gtag('config', 'G-LLMXBYB3P8');
    </script>

    <script is:inline>
      const getStoredTheme = () => {
        try {
          return localStorage.getItem('theme');
        } catch {
          return null;
        }
      };

      const allowedPalettes = ['oxford', 'stone', 'olive'];
      const getStoredPalette = () => {
        try {
          return localStorage.getItem('palette');
        } catch {
          return null;
        }
      };

      const paletteFromUrl = new URLSearchParams(window.location.search).get('palette');
      const resolvedPalette = allowedPalettes.includes(paletteFromUrl || '')
        ? paletteFromUrl
        : getStoredPalette() || 'oxford';

      try {
        localStorage.setItem('palette', resolvedPalette);
      } catch {}

      document.documentElement.dataset.palette = resolvedPalette;

      const preferredTheme = getStoredTheme();
      const resolvedTheme =
        preferredTheme ||
        (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');

      document.documentElement.classList.toggle('dark', resolvedTheme === 'dark');
      document.documentElement.dataset.theme = resolvedTheme;
    </script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap"
      rel="stylesheet"
    />

    <script type="application/ld+json" is:inline>
      {JSON.stringify({
        '@context': 'https://schema.org',
        '@type': 'ProfessionalService',
        name: 'controlthrive',
        founder: {
          '@type': 'Person',
          name: 'Servando Torres',
        },
        email: 'servando@controlthrive.com',
        url: baseUrl,
        description,
        sameAs: [
          'https://x.com/servandotorres',
          'https://linkedin.com/in/servandotorres',
          'https://github.com/servandotg',
        ],
        serviceType: [
          'Private capital workflow software',
          'Portfolio monitoring systems',
          'Decision support tooling',
        ],
      })}
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
